version: '3.8'
services:
  teamspace:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    deploy:
      resources:
        limits:
          memory: 6G
        reservations:
          memory: 4G
    environment:
      - NODE_ENV=development
